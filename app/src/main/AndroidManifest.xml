<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rocketman19.psychologistconsultationapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_boat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_boat"
        android:supportsRtl="true"
        android:theme="@style/Theme.PsychologistConsultationApplication">
        <activity
            android:name=".therapistRegistration"
            android:exported="true" />
        <activity
            android:name=".userRegistration"
            android:exported="true" />
        <activity
            android:name=".userLogin"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home"
            android:exported="true" />
        <activity
            android:name=".viewAllTherapists"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>